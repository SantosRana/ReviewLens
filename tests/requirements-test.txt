# tests/requirements-test.txt
# Test dependencies for ReviewLens
# Install: pip install -r tests/requirements-test.txt

pytest>=7.4.0
pytest-cov>=4.1.0          # Coverage reporting
responses>=0.24.0          # Mock HTTP requests for Naver API
freezegun>=1.2.0           # Freeze time for deterministic tests